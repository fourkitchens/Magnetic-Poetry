/**
 * @fileoverview Defines breakpoint information that will be shared
 * between the client and server.
 */

define({
  desktop: {
    minWidth: null,
    maxWidth: null,
    rowHeight: 29,
    charWidth: 8
  }
});
